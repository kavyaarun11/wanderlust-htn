import React from "react";
import { withRouter } from "react-router-dom";

function createTrip() {
  return (
    <div>
      <h1>Hello World</h1>
    </div>
  );
}

export default withRouter(createTrip);
